# Scoreboards

Curated result rows and public scoreboard views.

Use this section for public scoreboards tied either to real promoted eval packs
or to harness-level browsing boards that point at those packs.

The checked-in example smoke results are
`public_manifest_repair_smoke_v1.example.scoreboard.json` and
`homolog_contract_smoke_v1.example.scoreboard.json`, alongside the public map
summaries:

- `public_eval_harness_v1.example.scoreboard.json`
- `public_calibration_lanes_v1.example.scoreboard.json`
- `public_pressure_families_v1.example.scoreboard.json`
