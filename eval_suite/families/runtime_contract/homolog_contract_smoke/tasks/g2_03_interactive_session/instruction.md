Start an interactive Python REPL session. In that session, define a variable
`x = 21` and then evaluate `x * 2`. Read the output of that evaluation, and
write the resulting number (just the digits, on a single line) to a file
named `result.txt` in the current working directory. Then signal that the
task is complete.
