# Solver Pack

- Task pack: `fhard_07_original_tool_schema_workspace_mix`
- Canonical root: `/workspace/project`
- Authoritative state: `data/live_snapshot.ndjson`
- Decoy cache: `tmp/tool_cache/snapshot.json`

The hidden verifier checks both produced artifacts and schema-order evidence from trace events.
