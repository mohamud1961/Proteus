# Ceiling

Ceiling behavior for this sentinel:

- Uses canonical `python3`, `/workspace/runtime`, and expected runner command.
- Produces consistent runtime classification backed by verifier evidence.
- Avoids stale-doc drift and closes only after verification.
