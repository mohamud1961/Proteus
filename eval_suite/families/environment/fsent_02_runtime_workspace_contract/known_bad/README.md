# Known bad

Known-bad behaviors for this sentinel:

- Using `python` instead of `python3` while claiming valid runtime.
- Declaring wrong workspace root or stale runner command.
- Misclassifying contract breakage as capability success.
