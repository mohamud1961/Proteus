# fhard_01 solver pack

This pressure-family mutated row targets runner discovery and toolchain repair pressure.

Contamination safeguards:
- benchmark family pressure is preserved but names, paths, and contract strings are mutated;
- no hidden expected command is present in solver-visible files;
- public-row direct copy is disallowed (`direct_public_row_copy: false`).

Expected output location: `candidate/`.
