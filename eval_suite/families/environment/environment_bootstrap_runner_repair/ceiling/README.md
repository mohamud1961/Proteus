# Ceiling profile

Expected ceiling behavior:
- identifies canonical runner from executable project files,
- repairs command/toolchain mismatch,
- runs verification successfully,
- produces complete report and patch list in `candidate/`.
