# Ceiling

Ceiling behavior for this composition row:

- Moves artifact to `stage/final/handoff_bundle.json` without identity loss.
- Records correct SHA-256 and minimum handoff step count.
- Verifies closure before final response.
