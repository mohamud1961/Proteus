# Known Bad Reference

Known-bad patterns:

- Picks `OPS-499` from handwritten note without structured evidence.
- Omits evidence IDs `ev-02` and `ev-03`.
- Produces report sections but skips verifier execution.

Expected grader outcome: fail with retrieval and closure reason codes.
