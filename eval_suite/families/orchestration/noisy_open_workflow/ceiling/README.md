# Ceiling Reference

Ceiling behavior:

- Uses structured ticket log as source of truth.
- Selects `OPS-447` owned by `dax` with final-cycle closure evidence.
- Rejects `OPS-499` as distractor in the report.
- Includes required report sections and evidence IDs.
- Runs visible check and records verifier pass.

Expected grader result: pass with score 1.0.
