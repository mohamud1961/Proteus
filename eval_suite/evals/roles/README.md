# Role evals

Role evals are reserved for Architect, Solver, Runtime, and Verifier contract checks. The current unit and certification tests cover the public role boundaries without requiring a model or private task corpus.
