# Runtime Control Harness

Harness-level aggregation for the public smoke eval families.

This lane is for executable registry and board-manifest style artifacts that
tie multiple smoke families together, rather than for a single family leaf.

Current artifact:

- `runtime_control_harness_v1.json`

The canonical family leaves live under `../families/`.
