# Hooks

Hook lifecycle port for the Aether runtime.

These modules cover the session-hook matcher model and the permission hook
lifecycle around `PermissionRequest`, `PreToolUse`, and `PostToolUse`.

For public provenance guidance, see `docs/provenance/README.md` and
`docs/provenance/agent_runtime_adaptation_policy.md`.
